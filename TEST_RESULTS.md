# テスト結果

- 作成日: 2026-03-28 17:42 JST
- 更新日: 2026-03-28
- 作成者: Codex (GPT-5)

## 実施前提

- 実行環境: Windows 11 / `powershell.exe` 5.1
- 対象フォルダ: `C:\Work_Codex\ps1,batの変換器`

## 実施結果

### 正常系

- `bundle_system.ps1 -Mode Bundle` / `Restore` で、3ファイル混在の往復を実施しました。
- 対象: `alpha.bat`, `nested\beta.ps1`, `nested\gamma.bat`
- 結果: 集約終了コード `0`、復元終了コード `0`
- 確認内容: 相対パス再現、件数一致、復元後 SHA-256 一致、CRLF/LF の保持

### `.bat` 起動確認

- `bundle_files.bat` と `restore_files.bat` を `cmd.exe /c call` 経由で実行しました。
- 結果: 集約終了コード `0`、復元終了コード `0`
- 確認内容: `launcher.ps1` の復元を確認

### `test` フォルダ実地検証

- 入力元: `C:\Work_Codex\ps1,batの変換器\test`
- 対象件数: `.bat` / `.ps1` 合計 22 ファイル
- 結果: 集約終了コード `0`、復元終了コード `0`
- 確認内容: 全 22 ファイルで SHA-256 一致、相対パス一致、件数一致
- 備考: `bundle*.txt`、`thumbs.db`、`desktop.ini`、`~$*`、`*.tmp` を `input_files` に混在させても、除外ルールにより集約成功

### 異常系

| 観点 | 期待終了コード | 実績 |
| --- | --- | --- |
| `input_files` 0件 | `10` | `10` |
| 対象外拡張子混在 | `11` | `11` |
| `restore_input` 0件 | `20` | `20` |
| `restore_input` 複数件 | `21` | `21` |
| フォーマット識別子破損 | `22` | `22` |
| `bomType` 欠落 | `22` | `22` |
| 復元先衝突 | `24` | `24` |
| 危険な相対パス | `23` | `23` |
| 想定外拡張子混在 | `11` | `11` |

### 部分出力確認

- 復元先衝突時は既存の `a.bat` のみが残り、新規ファイルは出力されませんでした。
- 危険な相対パス検出時は `restore_output` が空のままで、部分出力は発生しませんでした。
- 親ディレクトリ位置にファイルがある場合は終了コード `24` で停止し、新規復元ファイルは作成されませんでした。

### 差異・既知制約

- 今回のテストでは、元ファイルと復元ファイルの差異は確認されませんでした。
- 集約ファイルは可逆性優先のため JSON + Base64 形式であり、人手での編集には向きません。
