# テスト結果

- 作成日: 2026-03-28 17:42 JST
- 更新日: 2026-03-28
- 作成者: Codex (GPT-5)

## 実施前提

- 実行環境: Windows 11 / `powershell.exe` 5.1
- 対象フォルダ: `C:\Work_Codex\ps1,batの変換器`

## 実施結果

### `Excel2LLM_Share` 実データ検証

- 入力元: `C:\Work_Codex\ps1,batの変換器\test\Excel2LLM_Share`
- 入力方法: `input_files\Excel2LLM_Share` として丸ごと配置
- 対象内ファイル: `.bat` 9 件、`.ps1` 13 件、`.md` 7 件、`.json` 1 件、`.bas` 1 件、合計 31 件
- 対象外ファイル: `.gitkeep` 2 件
- 結果: 集約終了コード `0`、復元終了コード `0`
- 確認内容: 31 ファイルで SHA-256 一致、10 フォルダで構成一致、`templates\Excel2LLM_Helper.bas` も復元対象として往復一致を確認
- 保存した集約ファイル: `C:\Work_Codex\ps1,batの変換器\output_bundle\bundle_saved_test_Excel2LLM_Share_with_bas_20260328_182357.txt`

### 正常系

- `test` フォルダを元に、`.bat` / `.ps1` / `.md` / `.json` の実ファイルと、追加作成した `.jsonl`、空フォルダを含む入力を構成しました。
- 対象入力: 23 ファイル、5 フォルダ
- 結果: 集約終了コード `0`、復元終了コード `0`
- 確認内容: 全 23 ファイルで SHA-256 一致、相対パス一致、フォルダ構成一致、空フォルダ `empty_dir_only` の復元

### `.bat` 起動確認

- `bundle_files.bat` と `restore_files.bat` を `cmd.exe /c call` 経由で実行しました。
- 結果: 集約終了コード `0`、復元終了コード `0`
- 確認内容: 通常復元に加え、`restore_files.bat structure` でフォルダ構成のみ復元できることを確認
- 補足: `--no-pause` を付けた自動実行でも、集約 `0`、復元 `0`、構成のみ復元 `0` を確認

### `test` フォルダ実地検証

- 入力元: `C:\Work_Codex\ps1,batの変換器\test`
- 対象件数: `.bat` / `.ps1` / `.md` / `.json` 合計 22 ファイル
- 結果: 集約終了コード `0`、復元終了コード `0`
- 確認内容: `test` 配下の許可拡張子はすべて往復成功
- 備考: 対象外ファイルを混在させた場合は、対象内ファイルが 1 件以上あれば対象外ファイル名を画面表示したうえでスキップして継続します

### 異常系

| 観点 | 期待終了コード | 実績 |
| --- | --- | --- |
| 対象外ファイルのみ | `11` | `11` |
| `bomType` 欠落 | `22` | `22` |
| 親ディレクトリ位置のファイル衝突 | `24` | `24` |
| フォルダ構成のみ復元 | `0` | `0` |

### 部分出力確認

- 親ディレクトリ位置にファイルがある場合は終了コード `24` で停止し、新規復元ファイルは作成されませんでした。
- `structure` モードではフォルダのみ作成され、ファイルは 1 件も出力されませんでした。

### 差異・既知制約

- 今回のテストでは、元ファイルと復元ファイルの差異は確認されませんでした。
- 集約ファイルは可逆性優先のため JSON + Base64 形式であり、人手での編集には向きません。
- `test` 配下には `.jsonl` の実ファイルがなかったため、`.jsonl` は同フォルダ配下に追加した検証用サンプルで往復確認しました。
